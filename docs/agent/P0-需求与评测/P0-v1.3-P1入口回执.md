# P0 v1.3 → P1 入口回执

revision：`p0-v1.3-20260809-ir-r1`
事件：`country_outage/2026-02-27 09:12:32/IR/1/r`
publication：`country_outage_publication_v1_989f698fb6f6c32579eebe7bb2bc833f`
collector：`rrc25`

## 一、交付结论

P0 已把当前页面、正式 API、publication/read model、后端候选和确定性推导分层记录，
并形成 26 项 Capability Discovery Ledger、8 项 unknown、10 个 Oracle seed 和 35 个
评测案例。

本回执不是 Tool Registry，也不是 P1 已实现证明。P1 必须基于 `adopt` 候选重新冻结
Capability Catalog、Typed Tool Contract、完整 Oracle、权限、状态和失败关闭策略。

## 二、adopt：17 项候选

建议 P1 选择：

- 事件/publication 绑定、身份、窗口、质量、生命周期；
- overview、前缀峰值、AS 峰值；
- IPv4、IPv6、新前缀时序和指标定义；
- AS 分页、指定 ASN、路径关联、有限路径样本；
- audit 身份；
- `series_extrema`、分单位地址族比较、事实时间线三个纯函数候选。

其中后三项只有 P0 feasibility/Oracle seed，建设期工具合同必须先行；运行时仍应先
完整理解用户目标，再把可执行部分 grounding 到登记算子。

## 三、defer：5 项

| 能力 | 原因 |
|---|---|
| trend | 当前 publication 返回 `publication_not_found` |
| 报告生成 | 当前 general page 不呈现，本轮未执行授权写旅程 |
| 现有追问 | 未证明符合 P1 开放目标与受控语义计划合同 |
| 跨事件组合调查 | 属于 P2 |
| Update/Withdraw 时序 | 当前 publication 没有对应轨道，缺失不能解释为 0 |

## 四、reject：4 项

- 外部证据：运行态 `not_configured`，且当前入口是 RRC25-only；
- 原因、责任、政府行为或 RCA；
- 真实用户、全国中断、流量和经济影响；
- DNS、HTTP、主动探测和其他外部数据源事实。

reject 指当前 P1 执行边界，不限制用户如何自然表达。用户可以自由问，但系统只能
诚实返回 supported、partial、clarify、unsupported 或 invalid_data。

## 五、P1 必须保留的确定性门

1. 先验证 `country_outage` 引用、incident、publication、revision 和 RRC25；
2. 用户目标保持开放，不由 Tool 白名单裁剪用户原意；
3. GroundingPlan 和执行算子封闭，只允许登记且通过 Oracle 的能力；
4. null、unknown、unavailable、not_configured 和 0 不得互换；
5. IPv4 地址与 IPv6 /48 等价块不得合并成单一绝对总量；
6. 路径相邻只表达观测关联，不表达依赖、传播或原因；
7. 计划、事实和证据身份全部通过后才能提交对话状态；
8. 多目标问题逐子目标裁决，不能因一部分可答而补齐另一部分；
9. 任何旧报告追问能力必须重新通过同一候选用户旅程，不能凭源码直接复用；
10. P1 通过不外推为 P2-P5。

## 六、P1 入口验收条件

- 17 个 adopt 候选被明确选择或再次 defer，不允许静默遗漏；
- 每个已选择能力有 Typed Tool Contract 和完整 Oracle；
- 35 个案例在同一候选上执行，参考事实、证据和边界均可复核；
- 5 个异常问题全部失败关闭；
- 8 项 unknown 有处理、保留或明确不阻塞决定；
- 当前 general page 的接入方式单独验收，不复用旧页面通过结论。

# P2 组合式调查正式入口回执

回执日期：2026-08-08

入口状态：允许启动 P2 需求与评测合同，不表示 P2 已实现

来源候选：`p1-candidate-61354911c7793d75`

P0 revision：`p0-v1-20260808-ir-r1`

P1 合同：`p1-contract-v1`

## 一、P2 可以继承的已验收合同

- 合法 `country_outage` 事件与 incident、publication、revision、cohort、RRC25、窗口、
  data-through 和 finality 的不可混用绑定；
- Conversation、Turn、幂等、取消、SSE 重放、到期与 revision 漂移语义；
- 规则优先、多标签 IntentPlan 与 `inherit / set / clear` 状态转换；
- `answerable / partial / clarify / unsupported / invalid_data` 五级可回答性；
- 当前页面/API 的概述、时间、峰值、范围、ASN、地址族、资源、路径、指标定义、证据身份
  白名单算子；
- AnswerEnvelope、Subanswer、EvidenceRecord、限制、未知、校验和运行身份；
- P0 35 例及 P1 浏览器、代理、Sidecar 和 Hook 验收制品。

## 二、P1 已正确拒绝或降级、应由 P2 重新立约的问题

- 一个问题需要多个白名单算子形成显式调查顺序、依赖和中间产物；
- 需要时间线、ASN、地址族、路径和资源之间的组合解释，而不只是并列回答；
- 需要记录“已查什么、下一步查什么、哪个子任务失败”的调查计划；
- 需要证据节点和关系，但不得把 P1 聊天历史直接当成 Evidence Graph；
- 当前 API 未暴露的 Update/Withdraw 仍应保持能力缺失，除非 P2 另有已验收数据入口。

## 三、不随回执转移的权限与能力

- 不转移外部网络、OONI、IODA、Cloudflare、DNS、HTTP 或流量数据访问权；
- 不转移模型自由选工具、动态发现工具、写操作、生产发布或部署权限；
- 不允许生成未经证据支持的假设、责任、全国断网、用户影响、经济损失或原因结论；
- 不表示 P3 有限假设、P4 多源证据或 P5 RCA 候选已经启动或通过；
- P1 的路径关联仍只是 RRC25 有序路径共同出现，不是依赖、传播或因果。

## 四、P2 启动前必须新增的合同

1. 冻结组合问题最小黄金集、调查计划 Schema 和逐步失败语义；
2. 定义 P2 白名单分析算子、依赖顺序、预算、取消与局部成功规则；
3. 定义 Evidence Graph 的节点、边、来源身份、时间对齐与禁止因果语义；
4. 用独立 P2 Hook 和候选身份验收，不改写 P0/P1 真值或回执；
5. 保持 RRC25-only，除非未来阶段通过新的数据源授权和验收。

回执结论：P1 已为 P2 提供有限、可追溯的正式入口；P2 必须作为新的工程合同建设，不能
通过扩写提示词或复用聊天历史绕过验收。

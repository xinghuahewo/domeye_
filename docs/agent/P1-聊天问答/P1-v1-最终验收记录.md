# P1-v1 最终验收记录

验收日期：2026-08-08

验收结论：通过

候选身份：`p1-candidate-61354911c7793d75`

实施源码 SHA-256：`61354911c7793d75889e6279c210ff59493aeec5207467259af516d4619437c4`

基线提交：`8eb33343166df6f98b97e80cad79cc21c24aff77`

P0 评测 revision：`p0-v1-20260808-ir-r1`

## 一、结论范围

本次通过只表示候选已经达到“RRC25 事件绑定聊天问答”P1 合同：用户从合法
`country_outage` 事件进入，在同一 incident、publication、revision、cohort、RRC25 和观测
窗口中询问当前页面/API 已有事实，并得到字段级证据、限制、未知和短期多轮状态。

本结论不表示已经部署或生产验证，不表示已经具备 P2 组合式调查、Evidence Graph、P3
有限假设、P4 多源证据或 P5 RCA 候选能力。

## 二、同一候选硬门禁

| 类别 | 结果 | 门禁 |
| --- | ---: | --- |
| 当前事实直接问题 | 20 / 20 | 全部通过 |
| 多轮旅程 | 5 / 5 | 全部通过 |
| 越界问题 | 5 / 5 | 禁止断言 0 |
| 缺失、冲突与异常 | 5 / 5 | 补零、快照混用和无效发布 0 |
| 事件绑定 | 100% | 通过 |
| publication/revision 绑定 | 100% | 通过 |

机器结果见 [candidate-result.json](../../../evaluation/country-outage/p1-v1/candidate-result.json)。

## 三、前端效果验收

| 要求 | 验收结果 | 证据摘要 |
| --- | --- | --- |
| P1-FE-01 | 通过 | 事件页显式入口；首屏固定显示 publication、revision、RRC25、data-through 与 finality |
| P1-FE-02 | 通过 | 身份轨、上下文、消息、输入、证据与限制层级独立 |
| P1-FE-03 | 通过 | 五级回答与排队、理解、完成、失败、取消状态有稳定文案 |
| P1-FE-04 | 通过 | 先结论后字段级证据，单位、限制和未知不隐藏 |
| P1-FE-05 | 通过 | 多标签结果按子目标展示，关键身份冲突失败关闭 |
| P1-FE-06 | 通过 | ASN 修正、指代与原因降级可见；旧实体已按状态转换清除 |
| P1-FE-07 | 通过 | 不唯一事件切换要求唯一引用，不默认最新事件 |
| P1-FE-08 | 通过 | SSE 重连与刷新恢复；不可恢复旧会话显示明确说明；取消不提交状态 |
| P1-FE-09 | 通过 | null、能力未暴露、无结果、身份冲突和无效序列不编码成 0 |
| P1-FE-10 | 通过 | 1440×1000 与 390×844 核心流程可达，Enter、Shift+Enter、状态朗读成立 |

浏览器机器记录见 [browser-acceptance.json](../../../evaluation/country-outage/p1-v1/browser-acceptance.json)，
截图见 [桌面](../../../evaluation/country-outage/p1-v1/browser-desktop.png) 与
[窄屏](../../../evaluation/country-outage/p1-v1/browser-narrow.png)。

## 四、后端与状态验收

| 要求组 | 验收结果 | 关键证明 |
| --- | --- | --- |
| P1-BE-01 至 P1-BE-03 | 通过 | 不可混用身份包络、规则优先多标签计划、确定状态机 |
| P1-BE-04 至 P1-BE-07 | 通过 | 白名单算子、确定性事实、五级可回答性、可重放回答包络 |
| P1-BE-08 | 通过 | 单写入、幂等、取消、SSE 重放、到期 410、revision 漂移 409 |
| P1-BE-09 | 通过 | 同源窄代理、loopback Sidecar、内部 Token 不进前端、外部来源字段拒绝 |
| P1-BE-10 | 通过 | 候选、规则、快照、阶段、错误与本地验收身份分开记录 |
| P1-BE-11 | 通过 | P0 35/35 硬门禁，不使用平均分抵消失败 |
| P1-BE-12 | 通过 | 本记录与 P2 入口回执只移交已验收 P1 合同和边界 |

## 五、测试与运行证据

- Agent Sidecar 全量测试：568 / 568 通过；
- P1 前端定向测试：12 / 12 通过，`vue-tsc --noEmit` 通过；
- Flask 窄代理测试：3 / 3 通过；
- P1 候选校验器测试：3 / 3 通过；
- P1 S0、S1、S2、S3 Hook：均为 exit code 0；
- 浏览器验收：12 / 12 通过；
- 冻结 `backend/core.sha256` 未修改，P1 没有改动冻结核心、数据或生产配置。

## 六、验收中发现并关闭的偏离

1. 首次浏览器多轮中，“谁造成的”正文已降级，但状态仍显示上一 ASN。已把该表达纳入
   boundary 转换，清除 ASN、地址族和指标，并把该状态加入多轮硬门禁；
2. 服务重启导致旧短期会话丢失时，页面原本静默新建。现明确显示旧会话不可恢复、当前
   已按同一事件新建，历史不会被静默改写；
3. 为复用同 lockfile 依赖建立的临时 `node_modules` 软链接被 Hook 判为 TASK 越界。链接
   已移除，不进入候选制品。

未保留阻断偏离。

## 七、发布状态分离

| 状态 | 结论 |
| --- | --- |
| implemented | 是 |
| tested | 是 |
| accepted | 是，本地候选验收 |
| deployed | 否，本任务未授权部署 |
| production_verified | 否 |

最终结论：候选 `p1-candidate-61354911c7793d75` 通过 P1-v1 验收，只可称为
“RRC25 事件绑定聊天问答”。

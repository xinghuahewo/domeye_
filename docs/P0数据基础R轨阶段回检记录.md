# P0 数据基础 R 轨阶段回检记录

最终效果合同：
[P0 数据基础 R 轨最终验收文档](./P0数据基础R轨最终验收文档.md)  
阶段计划：
[P0 数据基础 R 轨分阶段计划](./P0数据基础R轨分阶段计划.md)  
回检 Hook：`.codex/hooks/p0_data_foundation_r_track_review.py`

## 2026-07-27 整体合理性审查与状态更正

本次审查不改变最终效果合同，只修正“文档声明等于完成”和“范围决定等于出口通过”
两类错误解释。

| 阶段 | 修订后状态 | 本次处理 |
|---|---|---|
| R0 | `passed_after_correction` | 新增 37 表机器清单、校验器、证据哈希和条件化 Hook |
| R1 | `recorded_pass` | 保留既有只读报告，本次未重跑 |
| R2 | `recorded_pass` | 保留既有只读报告，本次未重跑 |
| R3 | `scope_stopped` | 60.24% 范围决定不再表述为标准出口通过 |
| R4 | `not_run` | R3 标准出口未通过，入口不成立 |
| R5 | `not_run` | R4 未执行，入口不成立 |
| R 轨最终状态 | `not_passed` | 只声明 `P0 历史数据兼容` |

本次新增规范交付物：

- `docs/data/P0数据基础R轨R0生产者身份清单.json`；
- `dev/data_quality/p0_producer_identity.py`；
- `dev/tests/test_p0_producer_identity.py`；
- `dev/tests/test_p0_r_track_hook.py`。

## R0：生产者身份补绑

日期：2026-07-26

### 实际结果

- 已确认数据层完全不含生产者身份：`database-inventory.json` 每表键仅
  `name`/`max_time`/`min_time`/`row_count`/`schema_hash`，全库 109 表 schema 中
  `version` 仅命中 `pg_dump` 自身 2 处，事实表无 `ingest_time`/`parse_time`/`detect_time`
  或算法版本列；`source varchar(2)` 为数据来源代码而非代码身份；
- 已确认两条恢复线索：原项目 Git 历史，以及带源文件行号、进程号与
  `set table name:` 绑定的运行日志；
- 已完成 37 张二三月表的逐表机器绑定，留空数为 0，登记两个不可恢复原因码
  `no_trusted_snapshot_at_process_start` 与 `process_predates_trusted_snapshot`；
- 已校准时区口径：日志为北京时间、Git 为 UTC，差值由日志 mtime 与末行时间戳交叉验证；
  初稿未换算导致的 HEAD 误判已更正；
- 检测侧代码内容身份 **0%** 可由 Git 恢复：仓库首次提交为 2026-03-07 08:52:30 UTC，
  而承担窗口后段全部产出的 PID 32082 于 2026-03-07 02:45:11 UTC 启动，早于仓库建立；
- 特征侧 2 月 0% 可恢复，3 月自 03-12 起可绑定 `6f01237`→`a6a138e`→`2e788cd`→`527b03e`；
- 原待复核 PID 已结案：2164894 有 insert，是 `527b03e` 段生产者；
  2188960、2189933 无 insert，不纳入生产者；
- 审计项 A1 判定为**对本窗口不成立**：`c0f3be3`（前缀过滤，2026-03-25 07:27:17 UTC）
  晚于窗口内最后一次特征进程启动（2026-03-25 06:54:53 UTC）33 分钟，从未被加载；
- 未修改生产数据，未重跑检测器，未执行重放，未修改 `backend/core/`。

### 边界与 RFA 回检

- RFA-01：机器校验返回 37/37、生产者组 4、证据 8、身份留空 0，成立；
- RFA-08：`backend/core/` 无改动，未更新哈希清单；
- RFA-09：全程只读，无数据库写操作，未触发任何激活；
- RFA-11：本阶段全部结论标注为代码事实或只读调查，未含已部署事实结论；
  权威卡片已随基线文档发布，并记录 `activation.performed=false` 与实际运行 prod-04 的不一致；
- 未以源精确对账结果宣称数据正确——本阶段未执行对账；
- 未以代码阅读结论宣称二三月数据已被污染或未被污染——A1 结论为
  "该代码路径未被加载"，属代码事实，不构成数据正确性判断；
- R0 未声称 R1 至 R5 任何出口已完成，未扩大到实时管线改造。

### 偏离与更正

- 初稿在未做时区换算的情况下判定 PID 32082 启动时 HEAD 为 `6f01237`，
  并据此写下"决策门未触发"。按 UTC 重算后结论相反：进程早于仓库建立，
  决策门**已触发**且范围大于预估。基线文档与绑定表均已更正，
  未通过改写最终验收要求消除该偏离。

### 阶段结论

P0 数据基础 R 轨最终验收回检：R0 一致（含一处已更正偏离）

### 下一阶段影响

- R2 必须调整：检测侧不能以代码版本建立归属，须以**有界原始重算**在候选环境
  重放小样本并与旧表逐项比对；特征侧 3 月 03-12 之后可用纯只读测量；
- 按目标顺序，R1 可与 R3 并行启动，R3 为长杆应尽早开始。

## R1：独立语义对账门禁

日期：2026-07-26

### 实际结果

- 门禁已实现并具名：`independent_semantic_reconciliation`（独立语义对账），
  实现于 `dev/data_quality/p0_independent_semantic_reconciliation.py`，
  报告显式声明 `"is_not": "source_exact_reconciliation"`；
- 7 条语义恒等式落地：AS/国家中断比率自洽、中断子集边界、持续时间自洽、
  时间顺序、特征聚合对账（F3 门禁化）、v4 地址口径自洽；
- 已在 `10.99.8.16:31627` 以 `domeye_core_reader` 只读角色 + 只读事务首次运行，
  产出可归档 JSON 报告并入库；
- 首次运行 `verdict: inconsistent`，登记 204 条：
  `as_outage_ratio_identity` 147、`feature_aggregate_identity` 46（真实 10,083/10,266）、
  `country_outage_ratio_identity` 4、`time_order_identity` 21 行；
  `duration_identity`、`outage_subset_bound`、`v4_address_identity` 通过；
- **审计项 F3 已由测量确认**：98.2% 的 collect 槽不满足全局=各国之和；
  样本槽回撤差 23,000（99.2% 未归属国家）；
- 已登记一项开放问题：P0 §9 将 `end_time < start_time` 阈值定为 0，
  而旧事实表实测 21 行，口径归属交 R2 判定；
- 命名审计：仓库 216 处「对账」均带限定词或上下文明确，未发现以无限定词
  「对账」指代正确性的表述；
- 未修改 `backend/core/`，零写操作，未修复门禁发现的任何问题。

### 边界与 RFA 回检

- RFA-02：源精确对账（复制保真）与独立语义对账（语义自洽）已分离命名、
  各有报告、互不替代，成立；
- RFA-09：只读角色 + `set_session(readonly=True)`，数据库写操作数为 0；
- RFA-11：本阶段结论类型标注为**数据测量**，与 R0 的代码事实区分；
  权威卡片已随门禁文档发布；
- 未以源精确对账结果宣称数据正确；
- 比率恒等式的"独立最大值"解释明确标注为**待 R2 确认的候选**，未据代码阅读结案；
- R1 未声称 R2 至 R5 任何出口已完成，未修复问题，未扩大到实时管线。

### 阶段结论

P0 数据基础 R 轨最终验收回检：R1 一致

### 下一阶段影响

- R2 已具备机器：F3 可直接引用本门禁测量结果结案，无需重新测量；
- R2 待办收窄为：比率三元组解释确认、S2 泄漏闩锁样本、S4 MOAS 归属，
  以及 §9 时间阈值口径归属；
- R3 仍未启动，为当前长杆。

## R2：遗留语义风险测量与结案

日期：2026-07-26

### 实际结果

- 测量实现于 `dev/data_quality/p0_legacy_risk_measurement.py`，只读执行，
  报告已归档；结案文档见 `docs/data/P0数据基础R轨R2遗留风险结案.md`；
- **F2 已确认（量级小）**：4 字节私有 ASN 在特征表出现 2,505 次 /
  37,711,780 行（0.0066%），而在中断类表出现 **0** 次。差分边界与代码边界一致，
  且 2 字节私有段两侧均为 0，构成同一数据集内的受控差分；
- **S2 已确认（量级大）**：路由泄漏事件 63,281 条，结束时间存在率 **0%**，
  是六类事件中唯一为 0 的类型（其余五类多数正常结束）。闩锁维度 2 月
  事件/前缀 = 1.000、3 月 = 1.178，确认强抑制，精确机制留待闭合；
- **S4 已确认存在偏置**：3 月 669,595 配对中 90.8% 归属等于候选字典序最小值
  （零假设约 50%）。方法学限制已随结论保留：多对多配对、仅覆盖 3 月、
  9.2% 等于字典序最大值无法由单一假设解释；归属机制未闭合；
- **S6 已排除**：规则日期 2026-04-06 晚于窗口；
- 未修改 `backend/core/`，零写操作，未修复任何问题。

### 边界与 RFA 回检

- RFA-03：三项均取得结论并附量级，S6 已排除，成立；
- RFA-09：只读角色 + 只读事务，写操作数为 0；
- RFA-11：结论类型标注为数据测量；
- **来自代码推断的结论数为 0**：F2 依受控差分、S2 依事件类型差分、
  S4 依统计偏置；S2 闩锁机制与 S4 归属机制两项残留均显式登记为
  待有界原始重算闭合，未以代码阅读补足；
- 未以源精确对账结果宣称数据正确；
- 未把缺失表示为 0——S2 的 0% 为实测存在率而非缺失填充；
- R2 未声称 R3 至 R5 任何出口已完成。

### 决策门判定

计划门条件「F2/S2/S4 全部以大量级确认则债务不可延后」**未全量触发**：
S2 量级大且确定，F2 量级小，S4 机制未闭合。

结论：不整体提升遗留管线债务优先级，但 **S2 单列为阻断项**——
本数据档下任何依赖路由泄漏结束时间或持续时间的下游指标、页面或报告，
须显式标注该维度不可用，不得把空值默认解释为"仍在进行"。

### 阶段结论

P0 数据基础 R 轨最终验收回检：R2 一致

### 下一阶段影响

- R0/R1/R2 三阶段已闭合，依赖链左支完成；
- 剩余为 R3 → R4 → R5 串行链，R3 为长杆且尚未启动；
- 两项残留（S2 闩锁机制、S4 归属机制）与 R1 登记的 §9 时间阈值口径问题，
  均需有界原始重算，可与 R3 的原始槽工作共用重算环境。

### 2026-07-27 下游语义约束执行

- **代码事实**：现有事件列表、详情和 Evidence Bundle 已统一增加
  `legacy_event_semantic_guardrails_v1`，未新增服务、表、依赖或独立门禁；
- **S2 约束**：路由泄漏生命周期标记为 `unavailable`，并阻断结束、时长、恢复及
  “仍在进行”结论；
- **S4 约束**：前缀中断归属标记为 `legacy_biased`，阻断责任 AS 与因果归属结论；
- **R1/F3 约束**：AS/国家中断比例标记为 `recompute_required`，存量比例不得作为
  权威事实；
- **本地验收**：后端 25 项、开发 Mock 12 项、前端归一化 9 项通过，前端类型检查
  通过，`backend/core` 哈希全部一致；
- **生产发布（2026-07-28）**：已从活动生产 `prod-04` 的不可变归档独立组装最小候选，
  发布为 `20260728T032500Z-legacy-semantic-guardrails-prod-05`；未以本地或服务器脏
  Git 工作区整体作为发布源；
- **生产身份**：后端实际 cwd 为
  `/home/bgpdata/Domeye-Core-runtime/releases/20260728T032500Z-legacy-semantic-guardrails-prod-05/backend`，
  监听 `127.0.0.1:28473`；Nginx `28471` 已返回本次前端入口与哈希资源；
- **生产验收**：事件样本 10/10 带护栏；金丝雀覆盖 S2、S4、AS/国家中断比例风险；
  固定样例的 incident ID、fact evidence ID 和字段计数跨版本不变；
- **发布边界**：未修改数据库、P0 数据发布、Nginx 配置和 `backend/core`；金丝雀
  `31631` 已停止，旧后端和旧前端树均完整保留为回滚点；
- **发布证据**：
  `/home/bgpdata/Domeye-Core-runtime/state/20260728T032500Z-legacy-semantic-guardrails-prod-05/`。

P0 数据基础 R 轨最终验收回检：R2 一致（下游约束已落实，遗留机制仍未闭合）

## R3：原始槽补齐与解析修复（清点完成，范围终止，标准出口未达成）

日期：2026-07-26

### 实际结果

- 槽位清点已复核并与基线一致：UPDATE 16,992 期望 / 10,238 准入 / 34 `parse_failed` /
  6,720 `source_unavailable`；RIB 177 / 104 / 3 / 70；合计准入覆盖率 **60.24%**；
- 磁盘复核确认自 2026-07-20 基线审计以来状态未变（`2026.02` 仍为 1,440 update）；
- **缺口形态查明为单一连续区间**：2026-02-01 00:00 至 2026-02-24 08:00（+08），
  23 天 8 小时 × 288 = 6,720，与计数精确吻合；非零散丢失；
- **`parse_failed` 归因**：30 个压缩流 EOF/CRC 失败 + 4 个空文件，
  即本地副本损坏而非源灭失，与 `source_unavailable` 成因不同，未合并登记；
- **上游可得性已核验**：对缺口区间内样本槽
  `data.ris.ripe.net/rrc25/2026.02/updates.20260210.0000.gz` 做 HEAD 得
  **200 / 6,984,275 字节**。6,790 个缺口槽在上游仍然可得；
- 补齐规模估算约 **60 GB**；
- 未下载任何制品，未写入数据，未修改 `backend/core/`。

### 边界与 RFA 回检

- RFA-04：**未成立**——出口条件"全部槽位或准入、或登记为不可得"未满足；
- RFA-09：全程只读，仅 2 次 HEAD 核验，无下载、无写入、无激活；
- RFA-10：补取完成前仍只声明 `P0 历史数据兼容`，未声明 `raw_traceable`；
- RFA-11：结论类型为数据测量与外部可得性核验；
- **未把可得的源登记为 `unrecoverable` 以凑出口**——这是本阶段最关键的边界：
  上游已核验可得，若登记为不可得即属以登记规避事实。

### 决策门判定（与预设不同）

门条件预设为"覆盖率远低于预期 → `raw_traceable` 可能不可达 → 停在历史兼容"。

实测覆盖率 60.24% 确属远低于预期，但**后半句不成立**：不可达的前提是源已灭失，
实测为源可得、本地未留存。因此**全窗口 `raw_traceable` 仍然可达**，
代价是一次约 60 GB 的受授权补取。

结论：不因覆盖率低而终止 R 轨；R3 的完成条件明确为一次受授权的补取作业。

### 范围决定与状态终止

工具 `dev/data_quality/p0_raw_slot_backfill.py`（dry-run）已独立枚举全窗口槽位，
结果与基线精确一致（16,992/177 期望、6,720/70 缺口），构成独立验证；
上游抽样 12/12 返回 200，按实测 `Content-Length` 修正补取规模为约 **45 GB**
（早先按本地均值估为 60 GB）。

**项目方决定（2026-07-26）：接受 60.24% 为最终，不执行补取。**

据此登记原因码 `backfill_declined_scope_decision`（6,790 槽）与
`local_artifact_corrupt`（37 槽）。**刻意不使用 `unrecoverable`**——
上游可得性已核验，把可得的源登记为不可得会使验收记录失真；
本项是范围决定，不是事实判断。

状态清点闭合：10,342 准入 + 6,790 范围决定 + 37 本地损坏 = 17,169，
未登记原因的未准入槽数为 0。RFA-04 的“状态与原因已知”要求成立；但 6,790 个
源仍可得的槽并未准入，也不是不可得，因此 R3 分阶段计划的标准出口**未达成**。
阶段状态为 `scope_stopped`，不得写成 `passed`。

### 阶段结论

P0 数据基础 R 轨最终验收回检：R3 范围终止（标准出口未通过）

### R 轨终止判定

按 RFA-10 与计划决策门：覆盖率 60.24% 远低于预期，且经范围决定不再追求补取，
因此**只声明 `P0 历史数据兼容`，不声明 `raw_traceable`**。

**R 轨在 R3 终止，不进入 R4/R5。** 二者出口均以全窗口原始追溯为前提，
在 60.24% 覆盖率下不可能诚实达成；按计划门的原意，此时应停在历史兼容，
而不是在无法闭合的声明上继续推进。

最终验收文档 RFA-01/02/03/07/08/09/11/12 已记录满足；RFA-04 的全窗口状态
与原因已知，但原始覆盖没有完成；RFA-05/RFA-06 为 `not_run`/未满足。
范围决定不改变二者的适用性，也不构成最终通过。

若将来需要 R4/R5，须先撤销该范围决定并完成补取，或另立明确声明为
"限定于已准入 10,342 槽"的窄口径目标（与本合同 RFA-05/06 不同，需新验收基准）。

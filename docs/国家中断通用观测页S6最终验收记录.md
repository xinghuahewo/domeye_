# 国家中断通用观测页 S6 最终验收记录

验收日期：2026-08-08  
最终候选：`20260807T201846Z-country-outage-general-page-prod27`  
候选提交：`604979f1584d34f7efa5b6110af8730f8a50bb7c`  
数据边界：`[2026-02-24T00:00:00Z, 2026-03-11T00:00:00Z)`，RRC25-only

## 一、最终结论

GFA-01 至 GFA-16 全部通过。伊朗与马拉维在同一不可变候选上完成数据、API、
桌面浏览器、移动浏览器、AS 同窗下钻和生产身份验收。当前 Backend、Frontend、
Git `codex/prod`、annotated tag、冻结读模型和生产进程均绑定 prod27；prod21 保留为
可执行回滚目标。

本阶段按影响范围判断后，没有补提或重跑数据。prod27 直接复用 S4 冻结通用读模型：

- dataset：`general_read_model_dataset_v1_63be5d12ef847d74824efe5be9892f8a`；
- manifest SHA-256：`1d1e0463af18cf1320ce37f918ed47fdfb4e1a4f78a73f198923db50a4fca5b9`；
- 81 个事件，13,488 个状态点；
- 伊朗为 3,455 个状态点、525 个受影响 AS、1,956 组路径关联；
- 马拉维为 57 个状态点、8 个受影响 AS、18 组路径关联。

数据库、Nginx、Sidecar 和付费模型均未改变。生产进程只读取既有受保护
`state.json` 中已验真的二三月只读数据库端口 `31627`，没有修改数据库、旧配置文件、
Nginx 配置或 Sidecar 身份。

## 二、同一候选身份

| 层次 | 最终身份或结果 |
| --- | --- |
| Git 生产主干 | `604979f1584d34f7efa5b6110af8730f8a50bb7c` |
| annotated tag | `20260807T201846Z-country-outage-general-page-prod27` |
| Backend | `20260807T201846Z-country-outage-general-page-prod27-backend` |
| Frontend | `20260807T201846Z-country-outage-general-page-prod27-frontend` |
| 候选清单 SHA-256 | `eaf627254ae95b5c2aab2ed5c43a46188c252e01c8483cff09a7de463f68c25d` |
| Canary 证据 SHA-256 | `c83e75eb3dd0e71e04b11c80b40823ea75dbcdb07ff18d2f94604d55192de76c` |
| Production 证据 SHA-256 | `34c21a98cc52cd04f5deb5c4440872917195bba51dad411956ff6d4755a37d09` |
| 浏览器证据 SHA-256 | `ba0acf510c87b49cb69247ebb04b50d4657787c1a4657e7f0a2bcf2a64e2ccd0` |
| 最终身份闭包 SHA-256 | `190d703e1fa5d6f515ea8dcc53c683efbc7bd1b52327d11ac2e223d493d01c3f` |

候选构建完成后，canary、production 和浏览器使用同一 Backend、Frontend 与冻结数据；
晋级期间没有重新构建制品。

## 三、GFA-01 至 GFA-16 结果

| 要求 | 最终效果证据 | 结论 |
| --- | --- | --- |
| GFA-01～04 | 81 个合法事件共用同一读模型和页面；窗口、cohort、peer ASN 方向、会话与路由不可见语义由 S1 冻结 | 通过 |
| GFA-05～06 | S2 唯一前缀、部分/完全中断、受影响 AS 与 AS 路由中断人口闭合 | 通过 |
| GFA-07～09 | 两国页面均显示前缀、AS、IPv4、IPv6 `/48` 四个趋势图，新前缀保持独立轨道 | 通过 |
| GFA-10 | 伊朗分页 525 个 AS，马拉维列出 8 个 AS；身份、性质、前缀、方向和路径规模同表展示 | 通过 |
| GFA-11 | AS48715 继承伊朗完整事件窗口，API 返回 540 个实际点并渲染 3 图；返回后恢复列表条件和滚动位置 | 通过 |
| GFA-12 | AS49666 的同期路径筛选来自真实 AS_PATH，首卡显示 3 条路径及前缀、IPv4、IPv6 `/48` 指标 | 通过 |
| GFA-13 | 普通页面内部身份、PRODUCT、PUBLICATION、REVISION、DATA THROUGH、Prefix×VP 和系统自证文案命中为空 | 通过 |
| GFA-14 | 错误 publication 为 404，非法路径语义和错误 AS 窗口为 400；非法事件不回退旧页面 | 通过 |
| GFA-15 | 最大响应 667,920 字节；AS 每页 20 条、路径每页 15 条；生产 p95 为 136.95ms | 通过 |
| GFA-16 | 伊朗与马拉维完成五类核心任务，重复、倒序和 8 路并发摘要一致，无因果或用户影响越界 | 通过 |

## 四、真实浏览器结果

生产浏览器使用 agent-browser Chromium 完成以下旅程：

- 伊朗桌面 1440×1000：4 个图表，无水平溢出，峰值 3,855 / 1,553 / 65,783；
- AS48715：继承精确事件窗口，无 500 或不可用状态，返回事件页后 `scrollY=1700`；
- 路径关联：按 AS49666 和“与中断同期出现”筛选，首卡有 3 条路径样本；
- 马拉维桌面：4 个图表、8 个 AS、18 组路径关联；
- 马拉维移动端 390×844：页面宽度 390，无水平溢出；
- 非法事件：显示“事件观测暂不可用”，0 个图表，不泄漏服务器路径或堆栈；
- 三个有效浏览器会话的 JavaScript error 均为 0；普通页面禁止文字命中为空。

截图及 SHA-256 已冻结到：

`/home/bgpdata/Domeye-Core-runtime/unified-releases/20260807T201846Z-country-outage-general-page-prod27/browser/`

## 五、生产与回滚

生产当前指针指向 prod27 Backend 和 Frontend，进程工作目录、release-id、读模型目录、
Frontend 字节树、Git 主干与 tag 均一致。`rollback-runtime.sh --check` 返回 `ready`，
回滚目标为 prod21 Backend 与 Frontend。生产 healthz、伊朗、马拉维和 AS48715 同窗 API
均已实测。

## 六、拒绝候选与纠偏记录

以下异常均没有被隐藏为最终通过：

1. S5 早期一次错误的候选停止命令使生产 Nginx 中断约 37 秒，随后由 systemd 恢复；
   该事故不属于 prod27 上线动作，但保留在最终记录中。
2. prod22 因运行目录错误，在 canary 就绪前失败；未切生产流量。
3. prod23 暴露生命周期身份缺口，并发现 Screen 命令行携带秘密参数；候选被拒绝，
   随后把秘密读取移入子进程。本文不记录任何秘密值。
4. prod24 未兼容旧 prod21 运行时身份，在任何生产变更前失败关闭。
5. prod25 首次切入后，浏览器发现 AS48715 同窗接口因误读旧端口 `29429` 返回 500；
   随即成功回滚 prod21。随后将数据库端口绑定到受保护 `state.json` 的 `31627`，
   并把 AS 同窗旅程加入机器门禁。
6. prod26 的运行结果已经正确，但验证器错误要求旧 AS 特征接口返回非合同 ETag；
   候选在 canary 阶段被拒绝，删除该假要求后形成 prod27。
7. prod27 的 canary、production、浏览器、生产身份和回滚检查全部通过。

这些纠偏只修改 S6 发布生命周期和验证门禁，没有改动 `backend/core/`、通用业务 API、
Frontend 源码、数据库、Nginx、Sidecar、S1～S4 数据制品或付费模型。

## 七、阶段封口

通用观测页最终验收回检：S6 已修正。

最终结论只对本文固定的 224-310、RRC25-only 范围成立，不扩展为多 collector、
全国断网、用户影响、原因、责任、客户依赖或 Network RCA 结论。

# 国家中断报告 Agent 2026-08-05 重认证记录

日期：2026-08-05  
范围：`deepseek-v4-flash`、Pi `0.82.1`、RRC25 国家中断报告核心剖面  
结论：第一次认证尝试安全失败；已完成离线修正和回归，尚未发起第二次付费尝试，
尚未晋级或部署。

## 一、授权与价格证明

- 用户授权范围：完整重认证与一次生产冒烟，累计费用硬上限 `3.30 CNY`；
- 价格来源：DeepSeek 官方价格页；
- 价格证据 SHA-256：
  `4af7fef5015e26cc6188778893dff29bab115bd06bb882ba22fdd8be57bb0003`；
- 观测时间：`2026-08-05T12:13:27.000Z`；
- 价格证明 ID：
  `price-attestation:2001d59ecefaa63d8ae1f2552700a3d3b062253c4660e0bb953090511deff448`；
- 价格证明资源 SHA-256：
  `f79931a49f191917e988082550d4704c8c60f6bae87b21017b022e6ff6e54a57`；
- 有效至：`2026-08-06T12:13:27.000Z`；
- 冻结价格：cache hit `0.0028`、cache miss input `0.14`、output `0.28`
  USD / 百万 token，cache write `0`。

调用前只读 readiness 为 `ready=true`，无 blocker、无开放预留，历史账本状态为
`resolved`。

## 二、第一次认证尝试

- 开始时间：`2026-08-05T12:16:08.212Z`；
- 结束时间：`2026-08-05T12:16:45.181Z`；
- 账本 reservation sequence：`35`；
- 账本 settlement sequence：`36`；
- 运行号：`1`；
- 结果：`rejected`；
- 正式拒绝码：`tool_execution_limit_exceeded`；
- 费用依据：`worst_case_reservation`；
- 本次记账：`0.5419008 CNY`；
- 结算后账本累计承诺：`14.781560640000002 CNY`；
- 结算后账本剩余：`5.218439359999998 CNY`；
- 开放预留：`0`。

认证在第一份报告中停止，没有自动重试，没有生成新认证 evidence 目录，没有修改
正式模型注册表，也没有切换生产。现有生产版本不受影响。

## 三、原因与修正

原协议只要求模型先调用 `country_outage_resolve` 和
`country_outage_get_observation`，并在两项结果齐备后强制 JSON 输出；工具阶段仍
允许可变模型自行选择工具，因此模型重复调用或误调用 ASN 时会触发冻结次数门。
门禁按设计失败关闭，但认证成功依赖模型偶然遵循提示，不具备足够确定性。

修正后不放宽任何工具、结果字节、供应商请求或 token 上限，而是在最终 payload
发送前由宿主强制执行三阶段协议：

1. 第一轮 `tool_choice` 固定为 `country_outage_resolve`；
2. resolve 成功后固定为 `country_outage_get_observation`；
3. 两项结果齐备后固定 `tool_choice=none`，并要求 JSON object 语言槽输出。

这样保留 Pi 工具调用和审计，同时把事实读取顺序从提示建议改为确定性协议。

## 四、离线验证

- 三阶段协议、发送前 payload、认证 runner 定向测试：`88 / 88` 通过；
- Sidecar 完整回归：`557 / 557` 通过；
- 真实 loopback OpenAI-completions adapter 已捕获并核验三轮实际 HTTP JSON：
  `resolve → observation → tool_choice=none + response_format=json_object`；
- 没有读取或输出认证文件内容；
- 没有发起第二次 provider 调用。

## 五、后续费用边界

第一次尝试已占用外部授权 `0.5419008 CNY`，授权剩余硬上限为
`2.7580992 CNY`。第二次完整认证的内部最坏预留为 `2.709504 CNY`，仍可在总
授权内启动；生产冒烟只有在认证按实际结算后仍至少保留单报告最坏预留时才允许
启动，否则必须再次取得用户授权。

本记录不是认证通过、模型晋级、候选发布或生产部署证明。

# P1.1 S0 合同候选

本目录保存受控 Semantic Planner 的 S0 语义对照集、P1-v1 冻结基线回执、不可变制品清单和阶段回执。

验证命令：

```bash
python3 dev/tools/validate_country_outage_p1_1_s0.py \
  evaluation/country-outage/p1-1-s0/manifest.json
```

通过只表示合同、fixture、对照集、基线摘要和 S0 阶段证据一致，不表示模型或运行主路径已实现。

# 仅在生成或刷新离线制品时使用。复制到仓库外并设置 chmod 600。
# 账号只需源数据库的只读权限。
SOURCE_DB_HOST=127.0.0.1
SOURCE_DB_PORT=5432
SOURCE_DB_NAME=bgp_project
SOURCE_DB_USER=domeye_snapshot_reader
SOURCE_DB_PASSWORD=change-source-password
